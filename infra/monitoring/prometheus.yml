global:
  scrape_interval: 15s

scrape_configs:
  - job_name: nexaiot-api
    static_configs:
      - targets: ['backend:3001']

# FUTURE IMPLEMENTATION:
# Add EMQX exporter, PostgreSQL exporter, and custom API metrics endpoint.
